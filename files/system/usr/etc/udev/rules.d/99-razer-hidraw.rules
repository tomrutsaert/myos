# Razer Viper V3 Pro — grant the logged-in user access to hidraw
# so userspace tools (razer-battery) can read battery via feature reports.
KERNEL=="hidraw*", ATTRS{idVendor}=="1532", ATTRS{idProduct}=="00c1", MODE="0660", TAG+="uaccess"
