# Compatibility target for the BlueBuild justfiles module.
# Fedora base images ship blujust under /usr/share/bluebuild, while the module
# still appends custom imports to the historical ublue-os path during builds.
