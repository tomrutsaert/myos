# Niri image SDDM defaults.
# The Fedora default references 01-breeze-fedora, which is not present in this image.
# Use an installed theme and an explicit cursor theme so the Wayland greeter shows a pointer.
[Theme]
Current=maya
CursorTheme=Adwaita

[General]
DisplayServer=wayland
