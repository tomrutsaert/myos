# Fedora's tuigreet stores --remember and --remember-session state here.
# The packaged greetd tmpfiles rule only fixes ownership/labels if it exists,
# but bootc images may not materialize RPM-owned /var/lib paths on first boot.
d /var/lib/greetd 0755 greetd greetd -
