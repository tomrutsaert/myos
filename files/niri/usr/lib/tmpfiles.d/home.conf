# Mask Fedora's default /home and /srv creation on bootc/ostree images.
# These paths are symlinks into /var, so the stock tmpfiles entries log
# "already exists and is not a directory" on every boot.
