modules:
  - type: dnf
    repos:
      cleanup: true
      files:
        - vscodium.repo
    install:
      skip-unavailable: true
      packages:
        - swappy
        - codium
        - chromium
        - fedora-chromium-config
