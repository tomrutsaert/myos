modules:
  - type: dnf
    # Keep the daemon enabled for boot without starting it during the build.
    env:
      SYSTEMD_OFFLINE: "1"
    repos:
      cleanup: true
      files:
        - protonvpn-stable.repo
    install:
      install-weak-deps: false
      packages:
        - proton-vpn-cli
